import React from 'react';

const Header = () => {
    return (
        <header>
            <h1 onClick={() => {console.log("Hello World !")}}>First project</h1>
        </header>
    )
}

export default Header;
